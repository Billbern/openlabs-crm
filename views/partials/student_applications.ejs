<div class="body-main container">
    <div class="jobs-area">
        <div>
            <% if (applied) { %>
                <% if ( applied.length > 0) { %>

                    <% applied.forEach(job => { %>
                        <div class="job-cards">
                            <div>
                                <img width="128" src="/img/learning/circle.png" alt="company logo">
                            </div>
                            <div class="cards-body">
                                <a class="job-link" href="/user/job/<%= job.id %>">
                                    <h6><%= job.title %></h6>
                                </a>
                                <div class="text_small">
                                   <%= job.city %>  <span class="dot"></span> <%= job.region %> <span class="dot"></span> Ghana
                                </div>
                                <div class="text_small">
                                    <%- job.content.slice(0, 199) %>...
                                </div>
                                <div>
                                    <span class="tag text_small">python</span> <span class="tag text_small">sql</span> <span class="tag text_small">pyqt5</span>
                                </div>
                                <div class="text_small">
                                    <span><%= moment(job.createdAt).fromNow() %></span>
                                </div>
                            </div>
                            <div class="cards-options">
                            </div>
                        </div>
                    <% }) %>
    
                 
                <% }else{ %>
                    <div style="position: absolute; color: #555; top: 50%; left: 45%;">
                        you have not applied for any job yet
                    </div>
                <% } %>
            <% }else{ %>
                <div style="position: absolute; color: #555; top: 50%; left: 45%;">
                    you have not applied for any job yet
                </div>
            <% } %>
        </div>
    </div>
</div>